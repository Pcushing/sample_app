<li>
  <span class="content"><%= wrap(micropost.content) %></span> <!-- Not sure why this isn't wrapping yet with microposts_helper's help -->
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
	<% if current_user?(micropost.user) %>
    	<%= render 'shared/delete_post', item_type: micropost %>
	<% end %>
</li>